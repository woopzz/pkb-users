services:

  app:
    build:
      context: ..
      dockerfile: Dockerfile.dev
    ports:
      - 8000:8000
    volumes:
      - ..:/app
    depends_on:
      - db
    env_file:
      - ../.env.dev
    tty: true
    command: /bin/bash

  db:
    image: postgres:17.6-bookworm
    ports:
      - 5432:5432
    volumes:
      - pkb-notes-db-data:/var/lib/postgresql/data
    env_file:
      - ../.env.dev

volumes:
  pkb-notes-db-data:
